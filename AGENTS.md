# Unignorable project contract

This is the provider-neutral instruction entrypoint for this repository. Portfolio behavior comes from the global agent core; this file adds only project-local facts.

- Canonical root: `/Users/mini-home/unignorable`.
- This directory owns an independent Git history. Do not treat parent folders, deployment mirrors, archives, or similarly named copies as editable authority.
- Before substantive changes, inspect the README, package metadata, and relevant project documentation for current commands and architecture.
- Keep runtime data out of Git and load application credentials only from `/Users/mini-home/.secrets`.
- Run the narrowest relevant tests or build before reporting completion.
- Deployment and remote publication are separate from local implementation and require current task authority.

## Reference context

Detailed prior project context is preserved in [PROJECT-CONTEXT.md](./PROJECT-CONTEXT.md). Read it when product history, architecture, or prior decisions affect the task. Dated status in that file is reference, not current authorization.
